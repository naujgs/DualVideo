---
phase: 260520-0dx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DualVideo/Features/Camera/CameraContentView.swift
autonomous: true
requirements:
  - SCREEN-SLEEP-01
must_haves:
  truths:
    - Screen stays on for the full duration of any recording session without user touch
    - Normal idle timer behavior resumes when the user leaves the camera view
  artifacts:
    - path: DualVideo/Features/Camera/CameraContentView.swift
      provides: Idle timer lifecycle management via onAppear/onDisappear
      contains: isIdleTimerDisabled
  key_links:
    - from: CameraContentView (SwiftUI lifecycle)
      to: UIApplication.shared.isIdleTimerDisabled
      via: onAppear / onDisappear modifiers on outermost view
      pattern: isIdleTimerDisabled\s*=\s*(true|false)
---

<objective>
Prevent iOS screen lock while the camera view is visible.

Purpose: Native camera apps hold the screen awake during recording. Without this, the idle timer fires and locks the screen mid-recording, interrupting the session.
Output: Modified CameraContentView.swift with idle timer disabled on appear and re-enabled on disappear.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable idle timer for camera view lifecycle</name>
  <files>DualVideo/Features/Camera/CameraContentView.swift</files>
  <action>
    In CameraContentView.swift, add two modifiers to the outermost view — the `GeometryReader` returned by `body` (the view that already has `.ignoresSafeArea()` and `.defersSystemGestures(on: .bottom)` chained to it at lines 220-221).

    Insert after `.ignoresSafeArea()` (line 220) and before `.defersSystemGestures(on: .bottom)` (line 221):

    ```swift
    .onAppear {
        UIApplication.shared.isIdleTimerDisabled = true
    }
    .onDisappear {
        UIApplication.shared.isIdleTimerDisabled = false
    }
    ```

    Do NOT place these inside the GeometryReader closure or on the inner ZStack — the existing `.onAppear` at line 208 is scoped inside GeometryReader and handles geometry sync. The new modifiers belong on the outermost view so they fire exactly once when CameraContentView enters and exits the hierarchy.

    No new imports are needed — UIApplication is available via SwiftUI (UIKit is already bridged in the existing alert code at line 270).
  </action>
  <verify>
    <automated>xcodebuild -scheme DualVideo -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | grep -E "error:|warning:|BUILD"</automated>
  </verify>
  <done>
    Build succeeds with no new errors. The string `isIdleTimerDisabled` appears in CameraContentView.swift. The modifiers are attached to the outermost view (after line 220), not inside the GeometryReader.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → UIKit | SwiftUI calls into UIApplication.shared — trusted internal Apple API, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0dx-01 | Denial of Service | isIdleTimerDisabled left true | accept | onDisappear guarantees reset; SwiftUI lifecycle is reliable for this pattern |
</threat_model>

<verification>
- `isIdleTimerDisabled = true` fires when CameraContentView appears
- `isIdleTimerDisabled = false` fires when CameraContentView disappears (e.g. user navigates away)
- Build compiles clean on both simulator and device targets
</verification>

<success_criteria>
Screen does not dim or lock while the camera view is visible. Normal idle timer resumes after leaving the camera view.
</success_criteria>

<output>
After completion, create `.planning/quick/260520-0dx-prevent-screen-sleep-during-camera-sessi/260520-0dx-01-SUMMARY.md`
</output>
