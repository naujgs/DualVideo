---
phase: 04-video-quality
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03-PLAN.md
files_modified:
  - DualVideo/Features/Recording/UI/TrimSheet.swift
  - DualVideo/Features/Recording/UI/TrimRangeBar.swift
  - DualVideo/Features/Camera/CameraContentView.swift
autonomous: false
requirements:
  - VQ-03
must_haves:
  truths:
    - "TrimSheet appears fullscreen after recording stops and pendingTrimURL is set"
    - "VideoPlayer shows the recorded clip in the top 60% of TrimSheet"
    - "TrimRangeBar has two independent sliders clamped so inPoint < outPoint with minimum 1s gap"
    - "In/out time labels update in real time as sliders move, formatted as m:ss (no leading zero on minutes)"
    - "Save Trimmed runs VideoTrimManager.trim(), shows Trimming... spinner, then calls saveRecording"
    - "Save Full calls saveRecording(url: originalURL) directly without trim export"
    - "Trim export failure shows Trim Failed alert with Save Full and Dismiss options"
    - "pendingTrimURL unreachable shows Save Failed alert with Dismiss only"
    - "Sheet disappears and pendingTrimURL is set to nil after either action completes"
  artifacts:
    - path: "DualVideo/Features/Recording/UI/TrimSheet.swift"
      provides: "TrimSheet SwiftUI view with AVPlayer preview, TrimRangeBar, action buttons, state machine"
      exports: ["TrimSheet"]
    - path: "DualVideo/Features/Recording/UI/TrimRangeBar.swift"
      provides: "Two-thumb range slider with track, selected range highlight, and 1s minimum gap"
      exports: ["TrimRangeBar"]
    - path: "DualVideo/Features/Camera/CameraContentView.swift"
      provides: "pendingTrimURL sheet presents TrimSheet (replaces Plan 03 placeholder)"
  key_links:
    - from: "TrimSheet Save Trimmed"
      to: "VideoTrimManager.trim()"
      via: "Task { await videoTrimManager.trim(sourceURL:range:) } → saveRecording"
      pattern: "VideoTrimManager"
    - from: "TrimSheet"
      to: "RecordingManager.saveRecording(url:)"
      via: "recordingManager.saveRecording(url: trimmedURL) on main actor after trim completes"
      pattern: "saveRecording"
    - from: "TrimRangeBar"
      to: "AVPlayer.seek"
      via: "onChange(of: inValue) { player.seek(to: CMTime(seconds: ..., preferredTimescale: 600)) }"
      pattern: "player.seek"
---

<objective>
Build the post-recording trim UI: `TrimRangeBar` (two-thumb range slider) and `TrimSheet`
(full-height sheet with AVPlayer preview, range sliders, action buttons, and full state
machine). Replace the Plan 03 placeholder in CameraContentView with the real TrimSheet.

Purpose: Complete VQ-03 — after every recording stops, the user sees a trim UI before the
clip is saved to Photos. They can trim to an in/out range or save the full clip.

Output: Two new UI Swift files, updated CameraContentView. Human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-video-quality/04-UI-SPEC.md
@.planning/phases/04-video-quality/04-CONTEXT.md
@.planning/phases/04-video-quality/04-03-SUMMARY.md

<interfaces>
<!-- Key contracts from Plans 01–03 -->
From DualVideo/Features/Recording/VideoTrimManager.swift (Plan 01):
```swift
actor VideoTrimManager {
    func trim(sourceURL: URL, range: CMTimeRange) async throws -> URL
}
enum TrimError: Error {
    case invalidRange
    case sessionUnavailable
    case exportFailed(Error?)
}
```
From DualVideo/Features/Recording/RecordingManager.swift (Plan 03):
```swift
@Observable final class RecordingManager {
    var pendingTrimURL: URL?
    func saveRecording(url: URL)      // internal — callable from TrimSheet
    func stopRecording(completion:)
}
```
From DualVideo/Features/Recording/UI/QualitySettingsSheet.swift (Plan 03 pattern — match style):
```swift
// Sheet presentation pattern established in Plan 03:
.presentationDetents([.large])
// No presentationDragIndicator for full-height sheets with explicit action buttons
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrimRangeBar and TrimSheet</name>
  <read_first>
    - .planning/phases/04-video-quality/04-UI-SPEC.md (TrimRangeBar and TrimSheet specs — AUTHORITATIVE)
    - .planning/phases/04-video-quality/04-CONTEXT.md (trim flow, error copy, state machine)
    - DualVideo/Features/Recording/UI/QualitySettingsSheet.swift (established sheet style to match)
    - DualVideo/Features/Recording/VideoTrimManager.swift (trim API — must call correctly)
    - DualVideo/Features/Recording/RecordingManager.swift (saveRecording signature)
  </read_first>
  <files>
    DualVideo/Features/Recording/UI/TrimRangeBar.swift,
    DualVideo/Features/Recording/UI/TrimSheet.swift
  </files>
  <action>
    === DualVideo/Features/Recording/UI/TrimRangeBar.swift ===

    import SwiftUI

    /// Two-thumb range slider for trim in/out point selection.
    /// UI-SPEC: track height 4pt, white.opacity(0.3) background, white selected range,
    /// 24pt circle thumbs with .black.opacity(0.4) shadow radius 4, minimum 1.0s gap.
    struct TrimRangeBar: View {
        @Binding var inValue: Double   // fractional [0.0, 1.0]
        @Binding var outValue: Double  // fractional [0.0, 1.0]
        let duration: Double           // total clip duration in seconds (for accessibility labels)

        /// Minimum trim duration: 1.0 second expressed as fraction of total duration.
        /// Guard: if duration is 0 or very short, fall back to a 0.01 minimum to prevent
        /// division by zero or inverted range.
        private var minimumGap: Double {
            guard duration > 0 else { return 0.01 }
            return max(1.0 / duration, 0.01)
        }

        var body: some View {
            GeometryReader { geo in
                ZStack(alignment: .leading) {
                    // Track background
                    Capsule()
                        .fill(Color.white.opacity(0.3))
                        .frame(height: 4)

                    // Selected range highlight
                    let trackWidth = geo.size.width
                    Capsule()
                        .fill(Color.white)
                        .frame(width: max(0, (outValue - inValue) * trackWidth), height: 4)
                        .offset(x: inValue * trackWidth)
                }

                // In-point (start) slider — underneath out-point slider in z-order
                Slider(value: $inValue, in: 0.0...(outValue - minimumGap))
                    .opacity(0.01)  // invisible but interactive — track rendered above
                    .frame(height: 44)  // 44pt touch target per HIG
                    .accessibilityLabel("Trim start")
                    .accessibilityValue(formatTime(inValue * duration))

                // Out-point (end) slider — on top in z-order
                Slider(value: $outValue, in: (inValue + minimumGap)...1.0)
                    .opacity(0.01)  // invisible but interactive
                    .frame(height: 44)
                    .accessibilityLabel("Trim end")
                    .accessibilityValue(formatTime(outValue * duration))
            }
            .frame(height: 44)
        }

        private func formatTime(_ seconds: Double) -> String {
            let totalSeconds = Int(seconds)
            let m = totalSeconds / 60
            let s = totalSeconds % 60
            return "\(m):\(String(format: "%02d", s))"
        }
    }

    NOTE: The invisible Slider approach (opacity 0.01) is a known SwiftUI pattern for
    custom-drawn sliders. The actual rendering (track + range) is drawn in the ZStack;
    the Sliders provide the gesture handling and accessibility. The two sliders overlap
    in frame — in-point slider has a lower z-order so out-point slider wins for taps
    near the right end. This matches RESEARCH.md A7 (two Slider views with overlap).

    === DualVideo/Features/Recording/UI/TrimSheet.swift ===

    import SwiftUI
    import AVKit
    import AVFoundation
    import CoreMedia

    /// Full-height sheet displayed after stopRecording when pendingTrimURL is non-nil.
    /// State machine: idle → trimming → (success: dismiss) / (error: alert shown)
    ///
    /// UI-SPEC:
    /// - .presentationDetents([.large]) — full height required for video preview
    /// - VideoPlayer fills top 60%
    /// - TrimRangeBar below player with in/out labels
    /// - "Save Trimmed" (primary) and "Save Full" (secondary) action buttons
    struct TrimSheet: View {
        let sourceURL: URL
        let recordingManager: RecordingManager

        @State private var player: AVPlayer
        @State private var duration: Double = 0
        @State private var inValue: Double = 0.0
        @State private var outValue: Double = 1.0
        @State private var trimPhase: TrimPhase = .idle
        @State private var showTrimFailedAlert = false
        @State private var showSaveFailedAlert = false  // pendingTrimURL unreachable

        private let trimManager = VideoTrimManager()

        enum TrimPhase {
            case idle
            case trimming
            case error(TrimError)
        }

        init(sourceURL: URL, recordingManager: RecordingManager) {
            self.sourceURL = sourceURL
            self.recordingManager = recordingManager
            self._player = State(initialValue: AVPlayer(url: sourceURL))
        }

        var body: some View {
            VStack(spacing: 0) {
                // Video preview — top 60% of sheet
                VideoPlayer(player: player)
                    .frame(maxWidth: .infinity)
                    .layoutPriority(1)

                // Trim controls — bottom 40%
                VStack(spacing: 16) {
                    // Range bar
                    TrimRangeBar(
                        inValue: $inValue,
                        outValue: $outValue,
                        duration: duration
                    )
                    .padding(.horizontal, 24)
                    .onChange(of: inValue) { _, newVal in
                        player.seek(
                            to: CMTime(seconds: newVal * duration, preferredTimescale: 600),
                            toleranceBefore: .zero,
                            toleranceAfter: .zero
                        )
                    }

                    // In/out time labels
                    HStack {
                        Text(formatTime(inValue * duration))
                            .font(.system(.footnote, design: .monospaced))
                            .foregroundStyle(.secondary)
                        Spacer()
                        Text(formatTime(outValue * duration))
                            .font(.system(.footnote, design: .monospaced))
                            .foregroundStyle(.secondary)
                    }
                    .padding(.horizontal, 24)

                    // Action buttons
                    HStack(spacing: 16) {
                        // Save Trimmed (primary)
                        Button {
                            saveTrimmed()
                        } label: {
                            if case .trimming = trimPhase {
                                HStack(spacing: 8) {
                                    ProgressView()
                                        .tint(.white)
                                    Text("Trimming…")
                                        .font(.system(.caption))
                                        .foregroundStyle(.secondary)
                                }
                                .frame(maxWidth: .infinity)
                                .frame(height: 44)
                                .background(Color.black.opacity(0.4))
                                .clipShape(Capsule())
                            } else {
                                Text("Save Trimmed")
                                    .font(.system(.body))
                                    .foregroundStyle(.white)
                                    .frame(maxWidth: .infinity)
                                    .frame(height: 44)
                                    .background(Color.black.opacity(0.4))
                                    .clipShape(Capsule())
                            }
                        }
                        .disabled({
                            if case .trimming = trimPhase { return true }
                            return false
                        }())

                        // Save Full (secondary)
                        Button("Save Full") {
                            saveFull()
                        }
                        .font(.system(.body))
                        .foregroundStyle(.secondary)
                        .frame(height: 44)
                        .disabled({
                            if case .trimming = trimPhase { return true }
                            return false
                        }())
                    }
                    .padding(.horizontal, 24)
                    .padding(.bottom, 24)
                }
                .background(Color(.systemBackground))
            }
            .presentationDetents([.large])
            .task {
                await loadDuration()
            }
            .onDisappear {
                player.pause()
            }
            // Trim failed alert (export error)
            .alert("Trim Failed", isPresented: $showTrimFailedAlert) {
                Button("Save Full") { saveFull() }
                Button("Dismiss", role: .cancel) {
                    if case .error = trimPhase { trimPhase = .idle }
                }
            } message: {
                Text("Could not trim recording. Try saving the full clip instead.")
            }
            // Save failed alert (file unreachable)
            .alert("Save Failed", isPresented: $showSaveFailedAlert) {
                Button("Dismiss", role: .cancel) {
                    recordingManager.pendingTrimURL = nil
                }
            } message: {
                Text("Recording file unavailable. The clip may have been removed.")
            }
        }

        // MARK: - Actions

        private func saveTrimmed() {
            guard case .idle = trimPhase else { return }
            trimPhase = .trimming

            let inPoint  = CMTime(seconds: inValue * duration, preferredTimescale: 600)
            let outPoint = CMTime(seconds: outValue * duration, preferredTimescale: 600)
            let range    = CMTimeRange(start: inPoint, end: outPoint)

            Task { @MainActor in
                do {
                    let trimmedURL = try await trimManager.trim(sourceURL: sourceURL, range: range)
                    // Delete original temp file after successful trim to avoid orphaned files
                    try? FileManager.default.removeItem(at: sourceURL)
                    recordingManager.saveRecording(url: trimmedURL)
                    recordingManager.pendingTrimURL = nil
                } catch {
                    trimPhase = .error(error as? TrimError ?? .exportFailed(error))
                    showTrimFailedAlert = true
                }
            }
        }

        private func saveFull() {
            recordingManager.saveRecording(url: sourceURL)
            recordingManager.pendingTrimURL = nil
        }

        // MARK: - Helpers

        private func loadDuration() async {
            // Guard: check file exists before loading asset
            guard FileManager.default.fileExists(atPath: sourceURL.path) else {
                showSaveFailedAlert = true
                return
            }
            let asset = AVURLAsset(url: sourceURL)
            if let d = try? await asset.load(.duration) {
                duration = d.seconds > 0 ? d.seconds : 0
            }
        }

        private func formatTime(_ seconds: Double) -> String {
            let totalSeconds = max(0, Int(seconds))
            let m = totalSeconds / 60
            let s = totalSeconds % 60
            // UI-SPEC: "0:03" format — m:ss, no leading zero on minutes
            return "\(m):\(String(format: "%02d", s))"
        }
    }
  </action>
  <verify>
    <automated>
      xcodebuild build -scheme DualVideo -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|Build succeeded" | tail -10
    </automated>
  </verify>
  <done>
    - DualVideo/Features/Recording/UI/TrimRangeBar.swift exists with TrimRangeBar struct
    - DualVideo/Features/Recording/UI/TrimSheet.swift exists with TrimSheet struct and TrimPhase enum
    - App builds without errors
  </done>
  <acceptance_criteria>
    - grep -n "struct TrimRangeBar" DualVideo/Features/Recording/UI/TrimRangeBar.swift returns a match
    - grep -n "minimumGap" DualVideo/Features/Recording/UI/TrimRangeBar.swift returns a match
    - grep -n "accessibilityLabel" DualVideo/Features/Recording/UI/TrimRangeBar.swift — both "Trim start" and "Trim end" present
    - grep -n "struct TrimSheet" DualVideo/Features/Recording/UI/TrimSheet.swift returns a match
    - grep -n "VideoPlayer(player:" DualVideo/Features/Recording/UI/TrimSheet.swift returns a match
    - grep -n "Trimming…" DualVideo/Features/Recording/UI/TrimSheet.swift returns a match
    - grep -n "Save Trimmed" DualVideo/Features/Recording/UI/TrimSheet.swift returns a match
    - grep -n "Save Full" DualVideo/Features/Recording/UI/TrimSheet.swift returns a match
    - grep -n "Trim Failed" DualVideo/Features/Recording/UI/TrimSheet.swift returns a match
    - grep -n "Recording file unavailable" DualVideo/Features/Recording/UI/TrimSheet.swift returns a match
    - grep -n "pendingTrimURL = nil" DualVideo/Features/Recording/UI/TrimSheet.swift — appears in both saveTrimmed and saveFull paths
    - xcodebuild build exits with "Build succeeded"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder in CameraContentView with real TrimSheet</name>
  <read_first>
    - DualVideo/Features/Camera/CameraContentView.swift (find the Plan 03 placeholder Color.clear block)
    - DualVideo/Features/Recording/UI/TrimSheet.swift (TrimSheet init signature just created)
    - DualVideo/Features/Recording/RecordingManager.swift (pendingTrimURL property)
  </read_first>
  <files>
    DualVideo/Features/Camera/CameraContentView.swift
  </files>
  <action>
    MODIFY DualVideo/Features/Camera/CameraContentView.swift:

    Find the pendingTrimURL sheet block added in Plan 03 (the one with `Color.clear.onAppear { ... }`).

    Replace the entire placeholder sheet content with TrimSheet:

    OLD (Plan 03 placeholder):
    ```swift
    .sheet(isPresented: Binding(
        get: { recordingManager.pendingTrimURL != nil },
        set: { if !$0 { recordingManager.pendingTrimURL = nil } }
    )) {
        if let url = recordingManager.pendingTrimURL {
            Color.clear.onAppear {
                recordingManager.pendingTrimURL = nil
                recordingManager.saveRecording(url: url)
            }
        }
    }
    ```

    NEW (real TrimSheet):
    ```swift
    .sheet(isPresented: Binding(
        get: { recordingManager.pendingTrimURL != nil },
        set: { if !$0 { recordingManager.pendingTrimURL = nil } }
    )) {
        if let url = recordingManager.pendingTrimURL {
            TrimSheet(sourceURL: url, recordingManager: recordingManager)
        }
    }
    ```

    No other changes to CameraContentView are needed.
  </action>
  <verify>
    <automated>
      xcodebuild build -scheme DualVideo -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|Build succeeded" | tail -10
    </automated>
  </verify>
  <done>
    - CameraContentView no longer contains `Color.clear.onAppear` placeholder
    - CameraContentView presents TrimSheet when pendingTrimURL is non-nil
    - App builds without errors
    - Full test suite exits 0
  </done>
  <acceptance_criteria>
    - grep -n "TrimSheet(" DualVideo/Features/Camera/CameraContentView.swift returns a match
    - grep -n "Color.clear.onAppear" DualVideo/Features/Camera/CameraContentView.swift returns nothing (placeholder removed)
    - xcodebuild build exits with "Build succeeded"
    - xcodebuild test (full suite) exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 4 — Video Quality and Export Options:
    1. Quality settings: QualitySettingsButton in left column → QualitySettingsSheet with Resolution + Quality pickers
    2. Trim UI: after recording stops → TrimSheet with video preview, range slider, Save Trimmed / Save Full
    3. Settings persist across app launches via UserDefaults
  </what-built>
  <how-to-verify>
    1. Build and run on a real device or simulator (iPhone 16, iOS 18).

    QUALITY SETTINGS:
    2. On the main camera screen, confirm a settings icon (slider.horizontal.3) appears ABOVE the torch button in the left column.
    3. Tap the settings icon while not recording — confirm QualitySettingsSheet slides up with:
       - "Resolution" section header + segmented picker with "720p" and "1080p"
       - "Quality" section header + segmented picker with "Low", "Medium", "High"
       - File size hint text showing ~37 MB/min / ~75 MB/min / ~112 MB/min depending on selection
    4. Change quality to 720p / Low. Dismiss sheet. Force-quit and relaunch app. Tap settings icon again — confirm 720p / Low is still selected (UserDefaults persistence).
    5. While recording (red dot visible): confirm settings icon is 50% opacity and tapping it does NOT open the sheet.

    TRIM SHEET:
    6. Record a 5–10 second clip. Tap Stop.
    7. Confirm TrimSheet appears as a full-height sheet with:
       - Video player at top (clip should be visible and playing)
       - Range slider with two handles
       - In/out time labels in m:ss format (e.g. "0:00" and "0:05")
       - "Save Trimmed" and "Save Full" buttons at bottom
    8. Drag the in-point handle to the right (e.g. 2 seconds in). Confirm:
       - In-point time label updates to "0:02"
       - Player seeks to the in-point position
       - Out-point handle cannot be dragged to within 1 second of in-point
    9. Tap "Save Trimmed". Confirm:
       - "Trimming…" spinner appears and both buttons are disabled
       - After export completes, sheet dismisses and "Saved to Photos" banner appears
       - Trimmed clip appears in Photos app at the expected shorter duration
    10. Record another clip. Tap Stop. When TrimSheet appears, tap "Save Full". Confirm:
        - Sheet dismisses immediately
        - "Saved to Photos" banner appears
        - Full-length clip is in Photos
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors verified, or describe specific issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pendingTrimURL file path → AVURLAsset | File in temp directory — may be removed before TrimSheet opens |
| Slider fractional values → CMTimeRange | Float arithmetic from Slider converted to CMTime; must not produce inverted/zero-length range |
| VideoTrimManager.trim output → PhotoSaveManager | Trimmed .mov in temp dir — original file deleted after successful trim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | Orphaned trimmed output file on export failure | mitigate | VideoTrimManager.trim() deletes outputURL in catch path (Plan 01). TrimSheet.saveTrimmed() deletes sourceURL after successful trim. |
| T-04-04-02 | Tampering | CMTimeRange constructed from Slider doubles — inverted range | mitigate | TrimRangeBar enforces minimumGap via Slider `in:` bounds. VideoTrimManager.trim() throws .invalidRange if inPoint >= outPoint (double-checked at export layer). |
| T-04-04-03 | Denial of Service | pendingTrimURL file missing before TrimSheet loads | mitigate | TrimSheet.loadDuration() guards FileManager.fileExists(atPath:) and shows "Save Failed" alert with "Recording file unavailable" copy if file is gone. Sets pendingTrimURL = nil on dismiss. |
| T-04-04-04 | Tampering | AVAssetExportPresetPassthrough fails silently | mitigate | VideoTrimManager guards session.status == .completed; throws TrimError.exportFailed on any other status. TrimSheet catches and shows "Trim Failed" alert with "Save Full" fallback. |
</threat_model>

<verification>
Full phase verification:

1. Build: `xcodebuild build -scheme DualVideo -destination 'platform=iOS Simulator,name=iPhone 16'` → "Build succeeded"
2. Tests: `xcodebuild test -scheme DualVideo -destination 'platform=iOS Simulator,name=iPhone 16'` → all green
3. Structural checks:
   - grep -n "TrimSheet(" DualVideo/Features/Camera/CameraContentView.swift → match
   - grep -n "Color.clear.onAppear" DualVideo/Features/Camera/CameraContentView.swift → no match
   - grep -n "VideoPlayer(player:" DualVideo/Features/Recording/UI/TrimSheet.swift → match
   - grep -n "15_000_000" DualVideo/Features/Recording/VideoQualitySettings.swift → match (High = 15 Mbps)
4. Human verify checkpoint — see Task 3
</verification>

<success_criteria>
1. TrimRangeBar.swift and TrimSheet.swift exist in DualVideo/Features/Recording/UI/
2. TrimSheet presents VideoPlayer, TrimRangeBar, time labels, Save Trimmed, Save Full
3. Save Trimmed runs VideoTrimManager.trim() with the user-selected CMTimeRange
4. Save Full bypasses trim and calls saveRecording(url: originalURL) directly
5. Trim export failure shows "Trim Failed" alert with Save Full fallback
6. File-unreachable case shows "Save Failed" alert with Dismiss only
7. pendingTrimURL is set to nil after either action (sheet dismisses)
8. App builds, all tests pass, human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-video-quality/04-04-SUMMARY.md`
</output>
